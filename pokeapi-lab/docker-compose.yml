services:
  db:
    image: mariadb:lts
    container_name: mariadb-estudio
    env_file:
      - db.env
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mariadb_data: